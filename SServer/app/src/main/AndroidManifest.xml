<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yt.s_server">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".user.LoginActivity"></activity>
        <activity android:name=".course.EolCourseMenuActivity" />
        <activity android:name=".course.EolHomeActivity" />
        <activity android:name=".home.ExamActivity" />
        <activity android:name=".home.ScoreActivity" />
        <activity android:name=".home.WorkActivity" />
        <activity android:name=".home.AttendanceActivity" />
        <activity android:name=".home.LeaveActivity" />
        <activity android:name=".home.ScheduleActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>